import { ModelCombinePipe } from './model-combine.pipe';

describe('ModelCombinePipe', () => {
  it('create an instance', () => {
    const pipe = new ModelCombinePipe();
    expect(pipe).toBeTruthy();
  });
});
